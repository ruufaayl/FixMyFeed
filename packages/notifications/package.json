{
  "name": "@fixmyfeed/notifications",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Notification delivery abstraction. First channel: a provider-neutral SMTP email adapter over an injected transport port (ADR-009).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@fixmyfeed/config": "workspace:*"
  },
  "devDependencies": {}
}
