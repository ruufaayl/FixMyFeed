{
  "name": "@fixmyfeed/connectors",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Versioned external-platform adapters (Shopify, WooCommerce, Google Merchant Center, SMTP). May depend on contracts and domain abstractions but not application UI.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {},
  "devDependencies": {}
}
